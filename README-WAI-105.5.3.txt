WORKSHOP AI — WAI-105.5.3
Visible Move Booking / Customer No Show Fix

Fixes:
- Adds a visible Move Booking button to active Service Manager job cards.
- Adds Customer No Show — Move Tomorrow for Waiting to Start jobs.
- Keeps the booking date visible on the tile.
- Preserves the existing move modal, capacity checks, technician selection and audit timeline.
- Corrects the later Customer Recognition card override that was removing the buttons.
- Adds cache-busting asset versions so Safari loads the updated files.

Open index.html to run the build.
